# 🗄️ راهنمای کامل راه‌اندازی Supabase — فارسی

## Supabase چیست؟

سوپابیس یک دیتابیس ابری **رایگان** است. اپ شما به این دیتابیس وصل می‌شود تا داده‌ها بین دو حساب **تحویل** و **تحول** همگام‌سازی شوند.

---

## مرحله ۱: ساخت حساب سوپابیس

1. به سایت [supabase.com](https://supabase.com) بروید
2. روی **Start your project** کلیک کنید
3. با **GitHub** یا **Gmail** ثبت‌نام کنید (رایگان)
4. روی **New Project** کلیک کنید
5. اطلاعات پروژه را وارد کنید:
   - **Name**: `delivery-app`
   - **Database Password**: یک رمز قوی بگذارید و **یادتان بماند!**
   - **Region**: نزدیک‌ترین منطقه (مثلاً `Middle East` یا `Europe`)
6. روی **Create new project** کلیک کنید
7. ⏳ **۱ تا ۲ دقیقه** صبر کنید تا پروژه ساخته شود

---

## مرحله ۲: ساخت جداول دیتابیس

1. در صفحه پروژه، از منوی سمت چپ روی **SQL Editor** کلیک کنید

2. روی **+ New query** یا آیکون ✏️ کلیک کنید

3. **تمام محتوای فایل `supabase-setup.sql`** را کپی کنید و در ادیتور پیست کنید

4. روی دکمه **▶ Run** (سبز رنگ پایین صفحه) کلیک کنید

5. اگر پیام **Success** دیدید ✅ — جداول ساخته شد!

6. برای تأیید:
   - از منوی سمت چپ روی **Table Editor** کلیک کنید
   - باید **۷ جدول** ببینید:
     - ✅ products
     - ✅ vehicles
     - ✅ deliveries
     - ✅ dispatches
     - ✅ dispatch_products
     - ✅ passwords
     - ✅ notifications

---

## مرحله ۳: کپی کردن آدرس و کلید پروژه

1. از منوی سمت چپ روی **⚙️ Settings** کلیک کنید

2. روی **API** کلیک کنید

3. دو مقدار مهم را کپی کنید:

   | مورد | کجا هست |
   |---|---|
   | **Project URL** | بخش `Project URL` — شبیه `https://xxxxx.supabase.co` |
   | **anon public key** | بخش `Project API keys` → ردیف `anon` `public` — یک رشته طولانی |

4. این دو مقدار را **جایگزین** مقادیر قدیمی در فایل `index.html` کنید:

```
var SUPABASE_URL = 'آدرس_پروژه_شما';
var SUPABASE_ANON_KEY = 'کلید_شما';
```

### مثال:
اگر:
- Project URL = `https://abc123.supabase.co`
- anon key = `eyJhbGciOi...`

آنگاه در `index.html`:
```
var SUPABASE_URL = 'https://abc123.supabase.co';
var SUPABASE_ANON_KEY = 'eyJhbGciOi...';
```

---

## مرحله ۴: فعال‌سازی Realtime (همگام‌سازی لحظه‌ای)

1. از منوی سمت چپ روی **⚙️ Settings** کلیک کنید

2. روی **API** کلیک کنید

3. به پایین اسکرول کنید تا بخش **Realtime** را ببینید

4. دکمه **Enable Realtime** را بزنید (اگر قبلاً فعال هست، نیاز نیست)

5. از منوی سمت چپ روی **Database** کلیک کنید

6. بخش **Replication** را پیدا کنید

7. مطمئن شوید جداول زیر **تیک** خورده‌اند:
   - ✅ products
   - ✅ vehicles
   - ✅ deliveries
   - ✅ dispatches
   - ✅ dispatch_products
   - ✅ notifications

---

## مرحله ۵: تست اتصال

1. فایل `index.html` را با آدرس و کلید جدید ذخیره کنید

2. آن را در گیت‌هاب‌پیجز آپلود کنید (یا روی `localhost` تست کنید)

3. اپ را باز کنید

4. وضعیت اتصال:
   - ✅ **☁️ متصل به سرور** = اتصال موفق
   - ❌ **آفلاین** = مشکلی هست

---

## ⚠️ نکات مهم

### درباره امنیت
- کلید `anon public` به صورت عمومی در کد هست — این **طبیعی** است
- جداول با RLS (Row Level Security) محافظت می‌شوند
- اپ شما قفل اصلی با رمز `7890` دارد — هیچکس بدون رمز وارد نمی‌شود
- اگر خواستید دسترسی را محدودتر کنید، در Settings → API می‌توانید RLS سخت‌گیرانه‌تر تنظیم کنید

### درباره رایگان بودن
- سوپابیس پلن رایگان **۵۰۰ مگابایت** فضا و **۵۰۰۰۰ ردیف** دارد
- برای اپ شما کاملاً کافی است ✅
- هیچ هزینه‌ای ندارد

### اگر اتصال قطع شد
1. بررسی کنید پروژه سوپابیس **Paused** نشده (در داشبورد)
2. اگر Paused بود → روی **Restore** کلیک کنید
3. آدرس و کلید در `index.html` صحیح باشند
4. اینترنت گوشی فعال باشد

---

## 📋 خلاصه مراحل

| مرحله | کار | زمان |
|---|---|---|
| ۱ | ساخت حساب و پروژه | ۲ دقیقه |
| ۲ | اجرای SQL ساخت جداول | ۱ دقیقه |
| ۳ | کپی آدرس و کلید | ۱ دقیقه |
| ۴ | فعال‌سازی Realtime | ۱ دقیقه |
| ۵ | تست | ۱ دقیقه |

**مجموع: حدود ۶ دقیقه** ⏱️